import React from 'react';
import './Cart.css'
const Cart = (props) => {
    return (
        <div>
            <div className='cart'>
            {/* <button onClick={clearAll}>clear all</button> */}
        </div>
        </div>
    );
};

export default Cart;